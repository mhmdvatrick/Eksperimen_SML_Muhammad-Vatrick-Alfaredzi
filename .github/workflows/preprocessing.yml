name: Data Preprocessing
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  preprocess:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run preprocessing script
        run: |
          cd preprocessing
          python automate_Muhammad_Vatrick_Alfaredzi.py

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          # Menyesuaikan nama folder output preprocessing Anda
          git add preprocessing/WA_Fn-UseC_-Telco-Customer-Churn_preprocessing/
          git commit -m "Automated preprocessing: Update clean data" || exit 0
          git push
